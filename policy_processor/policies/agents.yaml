# A2A policies — Sprint 2
#
# Each entry declares an agent and the remote agents it is allowed to call.
# Callers not listed here are treated as unknown and denied.

agents:
  agent-a:
    allowed_remote_agents:
      - agent-b

  agent-b:
    allowed_remote_agents:
      - agent-a
